@AGENTS.md

<!--
  Claude Code reads CLAUDE.md and does not read AGENTS.md. The import above pulls in the
  full agent instructions, so there is one source of truth and the two files cannot drift.

  This is an import rather than a symlink on purpose: a symlink needs Administrator rights
  or Developer Mode on Windows, while the import behaves the same on every platform.

  Add Claude-Code-only instructions below the import. Anything that should apply to Codex,
  Cursor, or Antigravity belongs in AGENTS.md instead.
-->
